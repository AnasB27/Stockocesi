<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - Stock O' CESI</title>
    <link rel="stylesheet" href="/stockocesi/static/css/connexion.css">
    <link rel="stylesheet" href="/stockocesi/static/css/main.css">
</head>
<body>
    {% include 'layout/header.twig' %}
    <main>
        <h1>Connexion</h1>
        <form action="/stockocesi/login" method="post">
            <div class="form-group">
                <label for="email">Adresse mail</label>
                <input type="email" id="email" name="email" required>
            </div>
            
            <div class="form-group">
                <label for="password">Mot de passe</label>
                <input type="password" id="password" name="password" required>
            </div>
            
            {% if error is defined and error is not empty %}
                <div class="error-message">{{ error }}</div>
            {% endif %}
            
            <button type="submit" class="main-button">Connexion</button>
        </form>
    </main>

    {% include 'layout/footer.twig' %}
</body>
</html>