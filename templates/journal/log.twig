<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="/stockocesi/static/css/gestion.css">
    <link rel="stylesheet" href="/stockocesi/static/css/main.css">
</head>
<body>
    {% include 'layout/header.twig' %}

    <main>
        <h1>Journal d'activité</h1>
        <div class="button-clear">
            <button class="button-clear">Effacer l'historique</button>
        </div>
        <ul>
            {% for task in tasks %}
                <li>
                    {{ task.task }} - Statut : {{ task.status }}
                    <form method="POST" action="/check-task" style="display:inline;">
                        <input type="hidden" name="id" value="{{ task.id }}">
                        <button type="submit">Terminer</button>
                    </form>
                    <form method="POST" action="/delete-task" style="display:inline;">
                        <input type="hidden" name="id" value="{{ task.id }}">
                        <button type="submit">Supprimer</button>
                    </form>
                </li>
            {% else %}
                <li>Aucune tâche trouvée.</li>
            {% endfor %}
        </ul>
    </main>

    {% include 'layout/footer.twig' %}
</body>
</html>